Copy this folder to ~/Desktop to run

Folder should contain:

    conda_scripts: (please make sure install scripts are named as [env_name].sh)
        codellama7binstruct.sh
        codeLlama34bInstruct.sh
    extra_install_scripts: (please make sure install scripts are named with the command required to run the program after installation. ex: zed.sh)
        davinci resolve script
        foxit reader script
        zedit script
    starship.toml
    .bashrc
    config.json (fastfetch)
    ?nvidia script? (opensuse_nvidia_script has not been tested) (others may need special consideration)
